import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

// Phase 6 mein poora navigation banayenge
// Abhi placeholder taake App.tsx compile ho sake

const AppNavigator = (): React.JSX.Element => {
  return <NavigationContainer>{/* Screens Phase 6 mein aayenge */}</NavigationContainer>;
};

export { AppNavigator };